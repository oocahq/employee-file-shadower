{
  "name": "employee-file-shadower",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^4.2.4",
    "@testing-library/react": "^9.3.2",
    "@testing-library/user-event": "^7.1.2",
    "crypto-js": "^3.1.9-1",
    "ramda": "^0.26.1",
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "react-file-reader": "^1.1.4",
    "react-scripts": "^3.4.1",
    "xlsx": "^0.15.4"
  },
  "scripts": {
    "dev": "PORT=4040 react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "firebase:target": "firebase target:apply hosting app $FIREBASE_HOSTING_SITE",
    "firebase:deploy": "firebase deploy --only hosting:app --token=$FIREBASE_TOKEN"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "eslint-config-airbnb": "^18.0.1",
    "firebase-tools": "^8.10.0"
  }
}
